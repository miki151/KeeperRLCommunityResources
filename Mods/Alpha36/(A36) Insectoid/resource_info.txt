# Resource definitions
# Make sure to add a new item and set resourceId = "X" in items.txt when adding a new physical resource.
# If there is no item or storageId then this is a non-physical resource.

# ResourceId        name             viewId (if visible in UI)   tutorialHighlight
"INSECT_CHEW"          {  "food"        { "ins_chew_pile" } }
"INS_BONE"             {  "bone"        { "bone" } }


